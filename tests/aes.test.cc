#include <gtest/gtest.h>
#include "aes.h"

// Unit tests for the AES class. Includes checks that full encryptions and
// decryptions run as expected.

TEST(AESInitTest, InitNoKey) {
    FAIL();
}

TEST(AESInitTest, InitWithKey) {
    FAIL();
}


// test the whole cipher end-to-end; encrypt and decrypt to get same plaintext back


// test implemented cipher modes
